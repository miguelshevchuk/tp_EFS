version: "3"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./src:/app/src
    ports:
      - "8000:8000"
    environment:
      - BD_SEMINARIO_DB=EFS
      - BD_SEMINARIO_USER=root
      - BD_SEMINARIO_PASSWORD=admin
      - BD_SEMINARIO_HOST=localhost
      - JWT_SECRET=My!@!Se3cr8tH4sh3
      - CLOUD_NAME=almacenceliacoapi
      - CLOUD_API_KEY=112718242582838
      - CLOUD_SECRET_KEY=dRhozsyc0zBM2nOg4ScHNCJnWd8 